[project]
name = "lbm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.10.6",
    "numpy>=2.2.6",
    "pandas>=2.3.2",
    "pydantic>=2.12.0",
    "pyvista>=0.46.3",
    "ruamel-yaml>=0.18.15",
]

[tool.setuptools.packages.find]
where = ["solver"]
